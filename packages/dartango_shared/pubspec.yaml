name: dartango_shared
description: Shared utilities and models for Dartango
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  json_annotation: ^4.8.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  test: ^1.24.0
  lints: ^3.0.0
